<p>
	<strong>Item:</strong>
	<%= @item.name %>
</p>

<p>
	<strong>Description:</strong>
	<%= @item.description %>
</p>

<p>
	<strong>Due Date:</strong>
	<%= @item.ends_on %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> | <%= link_to 'Back to list', items_path %>